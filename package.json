{
  "name": "profile-switcher-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static landing page and documentation for Profile Switcher.",
  "license": "MIT",
  "scripts": {
    "test": "node --test",
    "verify": "node --check script.js && node --test"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
